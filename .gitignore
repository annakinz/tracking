# Never commit app data: exports contain the family's items, notes,
# corrections history, and inline photos.
stratos-export*.json
*-export.json

node_modules/
.DS_Store

# local test scaffolding only — the app has no build step
scratchpad/
package.json
package-lock.json
